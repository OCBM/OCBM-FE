export type IconsPropType = {
  className?: string;
};

export type SquareIconPropTypes = {
  className?: string;
  critical?: boolean;
  low?: boolean;
  medium?: boolean;
  active?: boolean;
  color?: string;
};
